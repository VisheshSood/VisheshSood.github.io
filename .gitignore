# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
deploy.log
.deploy.lock
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# raw spec source docs (not used by the site; catalogue.json is the source of truth)
src/data/specs-raw/
src/data/specs-txt/

# PHP mailer secrets — created on the server only, NEVER committed
php-mailer/config.php
php-mailer/PHPMailer/

# editor / IDE settings (kept local, not in the public repo)
.vscode/

# agent / AI instructions (kept local, not in the public repo)
CLAUDE.md
AGENTS.md
